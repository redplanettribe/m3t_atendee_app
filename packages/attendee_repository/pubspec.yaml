name: attendee_repository
description: "Attendee repository implementation for the m3t attendee app."
version: 0.1.0
publish_to: "none"

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  domain:
    path: ../domain
  hive: ^2.2.3
  m3t_api:
    path: ../m3t_api

dev_dependencies:
  build_runner: ^2.4.13
  hive_generator: ^2.0.1
  mocktail: ^1.0.4
  test: ^1.25.0
  very_good_analysis: ^10.1.0
